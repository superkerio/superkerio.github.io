import { SidebarTransitionDirective } from './sidebar-transition.directive';

describe('SidebarTransitionDirective', () => {
  it('should create an instance', () => {
    const directive = new SidebarTransitionDirective();
    expect(directive).toBeTruthy();
  });
});
